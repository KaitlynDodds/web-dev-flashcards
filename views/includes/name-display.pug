.name-display
    p Welcome, 
        if username
            | #{username}
        else
            | student!
    
    if (username)
        a(href='/users/logout')
            i.fas.fa-sign-out-alt
    else 
        a(href='/users/login')
            i.fas.fa-sign-in-alt